import type { ScrapedProduct } from "../types";

export async function scrapeHmProductPage(url: string): Promise<ScrapedProduct[]> {
  // Stub implementation: H&M product page scraping not available in this build.
  // Return an empty array to allow compilation and safe runtime behavior.
  return [];
}
