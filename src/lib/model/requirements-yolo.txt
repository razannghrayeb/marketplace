# YOLOv8 Fashion Detection Service Requirements
# Using kesimeg/yolov8n-clothing-detection from Hugging Face
# Python 3.10+

# Core (HF weights: huggingface_hub + local path to YOLO(); do not use ultralyticsplus — it pins ultralytics<8.1.0)
ultralytics>=8.1.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
grpcio>=1.66.0
grpcio-tools>=1.66.0
protobuf>=5.27.0

# Image processing
pillow>=10.0.0
opencv-python-headless>=4.8.0

# PyTorch (CPU version - use GPU version for production)
torch>=2.0.0
torchvision>=0.15.0

# Utilities
numpy>=1.24.0
pydantic>=2.0.0

# DualDetector (dual-model-yolo.py): YOLO + Fashionpedia / transformers path
matplotlib>=3.7.0
transformers>=4.30.0
huggingface_hub>=0.16.0
timm>=0.9.0
